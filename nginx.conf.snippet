rtmp {
        server {
                listen 127.0.0.1:1935;
                chunk_size 4096;
                application live {
                        live on;
                        record off;
                        interleave on;

                        hls on;
                        hls_path /tmp/hls;
                        hls_fragment 15s;
                }
        }
}
