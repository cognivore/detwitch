rtmp {
        server {
                listen 127.0.0.1:1935;
                chunk_size 4096;
                application live {
                        live on;
                        record off;
                        interleave on;

                        hls on;
                        hls_path /var/www/detwitch/hls;
                        hls_fragment 15s;
                }
        }
}
